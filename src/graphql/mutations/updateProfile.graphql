mutation($description: String, $website: String){
    updateProfile(
        description: $description
        website: $website
        # file: $photo
    ){
        user{
            username
        }
    }
}